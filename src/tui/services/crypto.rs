//! TUI 加密服务适配器

pub struct TuiCryptoService;

impl TuiCryptoService {
    pub fn new() -> Self {
        Self
    }
}

impl Default for TuiCryptoService {
    fn default() -> Self {
        Self::new()
    }
}
